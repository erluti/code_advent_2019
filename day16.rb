# num.abs%10 will get last digit of num, regardless of pos/neg

# create "phaser" that will take an input and return the output, contains the base pattern internally
# examples will become tests

if __FILE__ == $0
  input = DATA.readline
end
__END__
